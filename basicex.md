# 随机步法代码解析

我使用了c++语言来解决这个问题。

## 头文件和全局变量

首先引用了所有需要用到的头文件，并且初始化了grid数组(存储行走的信息)和用于执行移动操作的数组pattern。

## 两个judge函数

judge函数判断当前位置是否可以走。
judge2函数判断当前位置是否为空，即判断当前位置的坐标是否在矩阵内。

## main函数

首先将grid数组初始化为'.'，并用pointX和pointY记录当前位置，同时走出第一步。随后在循环中，通过变量sum记录移动的步数。判断可走性后，为了使生成的随机方向可走，需用while(1)一直生成随机数，直到生成的随机数可走为止，此时更新pointX和pointY的值，并更新grid数组相应位置的值。别忘了跳出while(1)循环。当sum>25或因不可走而break时，输出grid数组。